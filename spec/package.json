{
  "name": "call-booking-spec",
  "version": "1.0.0",
  "private": true,
  "description": "TypeSpec source of truth for the Call Booking API. Generates api-contract.yaml (OpenAPI).",
  "type": "module",
  "scripts": {
    "generate": "tsp compile ."
  },
  "dependencies": {
    "@typespec/compiler": "^1.15.0",
    "@typespec/http": "^1.15.0",
    "@typespec/openapi3": "^1.15.0",
    "@typespec/versioning": "^0.85.0"
  }
}
